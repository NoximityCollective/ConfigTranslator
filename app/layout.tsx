import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ConfigTranslator - AI-Powered Minecraft Plugin Config Translator",
  description: "Free AI-powered translation service for Minecraft plugin configuration files. Supports MiniMessage color codes and preserves YAML/JSON structure. Made with love by Noximity.",
  keywords: "minecraft, plugin, config, translation, yaml, json, minimessage, noximity",
  authors: [{ name: "Noximity" }],
  openGraph: {
    title: "ConfigTranslator by Noximity",
    description: "Free AI-powered Minecraft plugin configuration translator",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
